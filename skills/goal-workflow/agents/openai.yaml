interface:
  display_name: "Goal Workflow"
  short_description: "Discover, approve, save, and launch Codex goals"
  default_prompt: "Use $goal-workflow to turn my rough task into an adaptively interviewed, explicitly approved, saved Codex goal, then ask again before starting it."
